#!/usr/bin/env bash
# Usage:
#  - eval $(~/.bb_vault.sh set)
#  - eval $(~/.bb_vault.sh unset)
if [ "$1" == "set"  ]; then
	echo '
	export VAULT_ADDR="https://vault.blueboard.io/"
	export VAULT_TOKEN="{{ (bitwarden "item" "bb_vault_token").notes }}"
	'
elif [ "$1" == "unset" ]; then
	echo "unset VAULT_ADDR
	unset VAULT_TOKEN"
else
	echo "Arg available is set or unset"
fi
